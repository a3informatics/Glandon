<%= stylesheet_link_tag 'tags'%>
<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Attributes</h3>
  </div>
  <div class="panel-body">
    <h5>Tags:</h5>
    <div class="tags-container" id="tags_container"></div>
    <% if policy(item.class).edit? and item.edit? and item.latest? %>
      <%= link_to "Update Tags", edit_tags_iso_managed_index_path(:id => item.id, :namespace => item.namespace), :class => "btn btn-primary", :id => "path_update_tags" %>
    <% else %>
    <% end %>
  </div> 
</div>
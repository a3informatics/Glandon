<div class="font-regular text-link"> Graph navigation </div>
To <span class="font-regular">move</span> along the graph in any direction, click-and-drag anywhere using the mouse. <br>
To <span class="font-regular">zoom-in and out</span>, use the mouse scroll. <br><br>

<div class="font-regular text-link"> Graph controls </div>
The following actions can be invoked by clicking their respective button or by pressing a key combination on the keyboard.

<ul>
  <li>
    <button class="btn white" style="margin:3px"><span class='icon-center'></span></button> or press <i>C</i>
    to <span class="font-regular">re-center</span> the graph to initial position and zoom level.
  </li>
  <li>
    <button class="btn white" style="margin:3px"><span class='icon-collapse-special'></span></button> or press <i>Shift + X</i>
    to <span class="font-regular">collapse all</span> Nodes (and their descendants) <span class="font-regular">except selected</span> Node.
  </li>
  <li>
    <button class="btn white" style="margin:3px"><span class='icon-collapse'></span></button> or press <i>Shift + C</i>
    to <span class="font-regular">collapse all</span> Nodes (and their descendants).
  </li>
  <li>
    <button class="btn white" style="margin:3px"><span class='icon-expand'></span></button> or press <i>Shift + E</i>
    to <span class="font-regular">expand all</span> Nodes.
  </li>
  <li>
    To <span class="font-regular">search</span> through displayed Nodes, use the Search bar.
    The Nodes with labels matching the search string will be <span class="text-accent-2">highlighted</span> as you type. <br>
    To <span class="font-regular">traverse the matching Nodes</span>, press <i>Enter</i> one or more times.
  </li>
</ul>

<div class="font-regular text-link"> Nodes </div>
A <span class="font-regular">Node</span> is a single vertex displayed on the graph and has a label and colored icon (representing its type). <br>

<ul>
  <li>
     <i>Hover</i> the mouse over a Node to <span class="font-regular">see details</span> about it in a tooltip.
  </li>
  <li>
    <i>Mouse left-click</i> on a Node to <span class="font-regular">select/ deselect</span> it. <br>
    <i>Arrow keys</i> (left, right, up, down) can be used to <span class="font-regular">select & traverse</span> Nodes in the graph .
  </li>
  <li>
    <i>Mouse right-click</i> or press <i>Spacebar</i> on a selected Node to <span class="font-regular">collapse / expand</span> its children (descendants). <br>
    When Node's children are collapsed, <span class='icon-plus text-xtiny text-light'></span> icon appears next to it.
  </li>
  <%= yield :extra_node_info %>
</ul>

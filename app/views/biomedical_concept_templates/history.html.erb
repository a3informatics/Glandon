<h3>History: <%= @identifier %></h3>
<div class="col-md-12">
	<table id="main" class="table table-striped table-bordered table-condensed">
		<%= render partial: "shared/stdInstanceHeader" %>
		<tbody>
			<% current_item = get_current_item(@bct) %>
			<% @bct.each do |item| %>
				<%= render partial: "shared/stdInstanceRow", :locals => {:item => item, 
	  				:current_item => current_item, 
	  				:showPath => biomedical_concept_template_path(:id => item.id, :namespace => item.namespace), 
	  				:searchPath => "", 
	  				:viewPath => "",
	  				:editPath => "",
	  				:clonePath => "",
	  				:deletePath => "" } %>
			<% end %>				
		</tbody>
	</table>
</div>


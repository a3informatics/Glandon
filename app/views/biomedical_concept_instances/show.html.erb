<% content_for :page_title do %> <%= "Show Biomedical Concept: #{@bc.has_identifier.identifier} (#{@bc.semantic_version})" %> <% end %>

<%= javascript_pack_tag 'biomedical_concept_instances/show' %>

<% third_level_breadcrumb(:biomedical_concept_instances, @bc, @close_path) %>

<div class="row">
  <%= render layout: "shared/headers/header_managed_item", :locals => {:item => @bc, :badge => "icon-biocon"} do %>
    <div class="actions-wrap">
      <%= render partial: "shared/buttons/return", :locals => { url: @close_path } %>

      <%= render layout: "shared/context_menu/menu", :locals => { id: "header-con-menu", color: "blue", side: "left"} do %>
        <%= render partial: "shared/context_menu/menu_item", :locals => { url: @edit_tags_path, id: "edit-tags-button",
          icon: "icon-tag", text: "Edit tags", disabled: @edit_tags_path.empty? } %>
      <% end %>

    </div>

  <% end %>
</div>

<div class="row">
  <div id="show-panel" class="col-md-12 card wide">
    <div class="card-header">
      Show: Biomedical Concept
    </div>
    <div class="card-content">
      <%= render partial: "shared/biomedical_concept_instances/show_panel", locals: { show_url: @show_path } %>
    </div>
  </div>
</div>

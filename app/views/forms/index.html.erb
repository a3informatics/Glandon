<h1>Forms</h1>
<div class="col-md-10">
	<h2>All Versions</h2>
	<table class="table table-striped table-bordered table-condensed">
	  <tr>
	    <th>Name</th>
	    <th>Identifer</th>
	    <th>Version</th>
	    <th></th>
	  </tr>
		<% @forms.each do |formId, form| %>
		  <tr>
		    <td><%= form.name %></td>
		    <td><%= form.identifier %></td>
		    <td><%= form.version %></td>
		    <td><%= link_to "Show", form_path(form.id), :class => "btn btn-primary" %></td>
		  </tr>
		<% end %>
	</table>
	<%= link_to "New version", new_form_path, :class => "btn btn-success" %>
</div>

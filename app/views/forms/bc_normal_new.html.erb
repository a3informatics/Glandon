<h3>New Placeholder Form</h3>
<div class="col-md-10">
	<%= form_for @form, :url => bc_normal_create_forms_path do |f| %>
		<div class="form-group">
	    	<%= label_tag :itemType, "Identifier" %><br />
	    	<%= text_field_tag "form[itemType]" %>
	  	</div>
		<div class="form-group">
	    	<%= f.label :identifier, "Identifier" %><br />
	    	<%= f.text_field :identifier %>
	  	</div>
	  	<table id="main" class="table table-striped table-bordered table-condensed">
	  		<tr>
	    		<th>Biomedical Concept</th>
	  			<th>Select</th>
	  		</tr>
	  		<% @bcs.each do |bc| %>
		  		<tr>
		    		<td><%= bc.label %></td>
		    		<td><%= check_box_tag "form[bcs][]", bc.id %></td>
		  		</tr>
		  	<% end %>
		</table>
	  	<%= f.submit "Create", class: "btn btn-default btn-primary" %>
	<% end %>
</div>
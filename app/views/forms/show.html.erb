<h1>Forms <%= @Form.name %> <small>(CDISC, <%= @Form.identifier %>, V<%= @Form.version %>)</small></h1>
<div class="col-md-10">
	<h2>Details</h2>
	<table class="table table-striped table-bordered table-condensed">
	  <tr>
	    <th>Identifer</th>
	    <th>Name</th>
	    <th>Version</th>
	  </tr>
	  <tr>
	    <td><%= @Form.identifier %></td>
	    <td><%= @Form.name %></td>
	    <td><%= @Form.version %></td>
	  </tr>
	</table>
</div>

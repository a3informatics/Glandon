<%= javascript_tag 'setPageTitle("Show Form: '+@form.has_identifier.identifier+' ('+@form.semantic_version+')")' %>

<%= stylesheet_link_tag 'shared/datatables_loading' %>
<%= javascript_pack_tag 'forms/show' %>

<% third_level_breadcrumb(:forms, @form, @close_path) %>

<div class="row">
  <%= render layout: "shared/headers/header_managed_item", :locals => {:item => @form, :badge => "icon-biocon"} do %>
    <div class="abcions-wrap">
    <%= render partial: "shared/buttons/ico_btn_secondary", :locals => {link_path: @close_path, id: "close",
      icon: "icon-long-arrow", btn_text: "Return", dt_toggle: "", disabled: false } %>
    </div>
  <% end %>
</div>

<div class="row">
  <div id="show-panel" class="col-md-12 card wide">
    <div class="card-header">
      Show: Form
    </div>
    <div class="card-content">
      <%= render partial: "shared/forms/show_panel", locals: { show_url: @show_path } %>
    </div>
  </div>
</div>

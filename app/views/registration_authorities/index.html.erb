<h3>Registration Authorities</h3>
<div class="col-md-12">
	<h4>Owner:</h4>Repository owner is <%= @owner.shortName %></p>
	<h4>Details:</h4>
	<table id="main" class="table table-striped table-bordered table-condensed">
		<thead>
			<tr>
			    <th>Number</th>
			    <th>Scheme</th>
			    <th>Name</th>
			    <th>Short Name</th>
			    <th>CID</th>
			    <th></th>
		  	</tr>
		</thead>
	  	<tbody>
			<% @registrationAuthorities.each do |key, registrationAuthority| %>
		  		<tr>
				    <td><%= registrationAuthority.number %></td>
				    <td><%= registrationAuthority.scheme %></td>
				    <td><%= registrationAuthority.name %></td>
				    <td><%= registrationAuthority.shortName %></td>
				    <td><%= registrationAuthority.id %></td>
				    <td><%= link_to "Delete", registration_authority_path(registrationAuthority), :class => "btn btn-danger btn-xs", method: :delete, data: { confirm: "Are you sure?" } %></td>
				</tr>
			<% end %>
		</tbody>
	</table>
	<%= link_to "New Authority", new_registration_authority_path, :class => "btn btn-success" %>
</div>
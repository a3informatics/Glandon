$(document).ready(function(){

  var ccp = new ChangesCdiscPanel({url: "", data: ""});
  var dp = new DifferencesPanel(null);
  var cp = new ChangesPanel(null, 2);

  var selectCallback = function(r){
    dp.reload(r.differences_url);
    cp.reload(r.changes_url);
  }

  ccp.sCallback = selectCallback;
});

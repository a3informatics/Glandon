$(document).ready( function() {
  var columns = [
    {"data" : "owner"},
    {"data" : "identifier"},
    {"data" : "label"},
  ];

  var ip = new IndexPanel(indexUrl, strict_param, columns, count);
});

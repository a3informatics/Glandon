$(document).ready( function() {
  var hp = new HistoryPanel(url_path, strict_params, identifier, scope_id, 20);

  $(".icon-context-menu").on("click", function() {
    $(this).find(".context-menu").toggleClass("collapsed");
  });
});

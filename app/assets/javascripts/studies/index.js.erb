$(document).ready( function() {
  var columns = [
    {"data" : "owner"},
    {"data" : "identifier"},
    {"data" : "label"},
  ];

  var ip = new IndexPanel(studiesUrl, "study", columns, 1000);
  new NewStudyModal();

});

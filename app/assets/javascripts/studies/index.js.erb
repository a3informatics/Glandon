$(document).ready( function() {
  var columns = [
    {"data" : "owner"},
    {"data" : "identifier"},
    {"data" : "label"},
  ];

  var ip = new IndexPanel(studiesIndexUrl, "study", columns, -1);
  new NewStudyModal();

});

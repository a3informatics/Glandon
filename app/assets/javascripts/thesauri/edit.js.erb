$(document).ready(function() {
  ep = new EditorPanel("1", children_path, update_path, edit_path, delete_path);
  np = new NewPanel(add_child_path, callback);
})

function callback() {
  alert("Callback")
}
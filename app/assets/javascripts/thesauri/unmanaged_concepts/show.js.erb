$(document).ready( function() {
  var lp = new LinksPanel(syn_url, syn_panel_id);
  var pp = new LinksPanel(pt_url, pt_panel_id);
  if (url_path !== "") {
    var columns = [
      {"data" : "identifier"},
      {"data" : "notation"},
      {"data" : "preferred_term"},
      {"data" : "synonym"},
      {"data" : "extensible"},
      {"data" : "definition"}
    ];
    var cp = new ChildrenPanel(url_path, 20, columns);
  }
});

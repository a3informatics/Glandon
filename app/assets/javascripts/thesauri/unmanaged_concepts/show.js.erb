$(document).ready( function() {
  if (url_path !== "") {
    var columns = [
      {"data" : "identifier"},
      {"data" : "notation"},
      {"data" : "preferred_term"},
      {"data" : "synonym"},
      {"data" : "extensible"},
      {"data" : "definition"}
    ];
    var cp = new ChildrenPanel(url_path, 20, columns);
  }
});

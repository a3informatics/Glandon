$(document).ready( function() {
  var columns = [
    {"data" : "identifier"},
    {"data" : "notation"},
    {"data" : "preferred_term.label"},
    {"data" : "synonym_list"},
    {"data" : "extensible"},
    {"data" : "definition"}
  ];
  var cp = new ChildrenPanel(url_path, 20, columns);
});

$(document).ready( function() {
  var columns = [
    {"data" : "owner"},
    {"data" : "identifier"},
    {"data" : "label"},
  ];

  var ip = new IndexPanel(bcsIndexUrl, "biomedical_concept", columns, -1);

});
